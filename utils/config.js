/**
 * 与 code/minigame/src/config.js 一致：基址指向 /api/minigame，路径与 admin Minigame*Controller 对齐。
 */
module.exports = {
  apiBaseUrl: 'https://game.jyiai.com/api/minigame',
  /** 请求体 source 字段，与后端 MinigameStorageServiceImpl 默认 minigame 一致 */
  apiSource: 'minigame',
}
